{
  "version": 2,
  "buildCommand": "pnpm install && pnpm --filter mcp-server db:generate && pnpm --filter mcp-server build && pnpm --filter ui build",
  "installCommand": "pnpm install",
  "framework": null,
  "outputDirectory": "dist/ui",
  "rewrites": [
    {
      "source": "/ui/(.*)",
      "destination": "/ui/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
